spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db-test
    username: user
    password: password
    schema: ppLibrary
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true